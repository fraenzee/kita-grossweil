---
// Importiere die Funktion, um Weiterleitungen zu handhaben
import { Astro } from 'astro';

// Sende eine Weiterleitung zur Admin-HTML-Datei
const redirectPath = '/admin/index.html';
return Astro.redirect(redirectPath);
---
